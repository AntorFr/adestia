{
  "schemaVersion": 1,
  "id": "lots",
  "kind": "app",
  "description": "The chantiers of a galaxy of repositories: every .agent/lots/ fiche read from git, merged into one graph, and drawn as who has the hand, what is blocked and what a decision would unfreeze. Read-only — it never writes a fiche.",
  "contract": 1,
  "view": "./web/app.js",
  "styles": [
    "./web/lots.css"
  ],
  "api": "./api.mjs",
  "secrets": [
    "LOTS_REPOS"
  ],
  "tile": {
    "label": "Lots",
    "icon": "🚧",
    "hue": "indigo"
  }
}

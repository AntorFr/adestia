# Questions — chantier concepts

> Une question vivante à la fois ; les autres attendent leur tour, dans l'ordre.

| # | Question | État | Réponse |
|---|---|---|---|
| 1 | Créer la worktree `concepts` (branche du même nom) pour ce chantier ? | close | oui (15/09) |
| 2 | Pas 1, le chat : accepter que, quand le magasin ne répond pas après un tour perdu, l'onglet garde ce qu'il a dessiné avec l'erreur, et rien de plus (aujourd'hui il classe localement les parties reçues) ? | pris par défaut (15/09) : oui, il garde le dessin avec l'erreur | à renverser d'un mot |
| 3 | Pas 2 : les tours sans surveillance passent par le bureau ; le chemin « tour sans fil » de `/api/turn` est supprimé (un tour exige un fil) ou gardé comme affordance d'API nommée ? | pris par défaut (15/09), révisé en cours de pas : bureau pour tous ; le tour sans fil est **gardé et nommé** — c'est la question éphémère (mode « éphémère » de la barre de parité, que la coque n'offre pas encore ; un test du serveur le documentait déjà) ; l'onglet, lui, ne l'emprunte plus : un fil impossible à créer est dit sous le message | à renverser d'un mot |
| 4 | Pas 3 : le mot retenu pour le fil — « thread » (conception) ou « conversation » (API) — pour les identifiants du code ; l'API HTTP ne change pas. | pris par défaut (15/09) : « conversation » partout, « session » réservé à la session moteur. Précisé en cours de pas : la session de connexion (cookie `adestia_session`, clé `sessionSecret`, `request.session`) garde le mot du web, c'est un contrat ; le champ `sessionId` du flux d'armement reste sur le fil (le type serveur devient `ArmingFlow`) ; les classes CSS suivent (`adestia-conversations`), l'outillage du banc (`threadsDir`) et les plugins ne bougent pas ; les mots propres aux moteurs (le *thread* de Codex, la *session* du CLI) restent | à renverser d'un mot |
| 5 | Pas 5 : `collections` rendu par le cœur — on l'ouvre dans ce chantier ou on le laisse au suivant ? | **renversée par le propriétaire (16/09)** : « oui, on dit la même chose, ok tu peux retirer le plugin » — le cœur dessine `type: collection`, le plugin est retiré | close |
| 6 | Pas 5, les choix pris en construisant : pas d'écran ni de tuile « Collections » (une collection se trouve là où elle est classée) ; `into:` donne un ＋ qui DEMANDE à l'agent de créer le membre ; la facette ouverte est un état de l'écran, pas une adresse ; les configs d'instance qui listent encore `collections` dans `extensions.apps` le retirent au déploiement (le serveur le signale au démarrage). | pris par défaut (16/09) | à renverser d'un mot |
